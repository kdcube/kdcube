# Metrics service
fastapi==0.116.1
# Bound with fastapi: the Project Board source family installs mcp into every
# image, and an unpinned sse-starlette resolves to a release that needs
# starlette 1.x, which fastapi 0.116 refuses. Same pin as chat-processor.
sse-starlette==3.0.3
uvicorn==0.35.0
httpx
aiohttp==3.12.15
requests==2.32.5
python-dotenv==1.1.1
pydantic==2.11.7
pydantic-settings==2.10.1
redis==6.2.0
asyncpg
psycopg2-binary==2.9.10
Pillow

# Shared descriptor and authority configuration.
connection-hub>=2026.09.23.0158,<2027

# CloudWatch export
-r requirements-aws.txt

# Prometheus pushgateway export
prometheus-client

orjson
bs4

# Remove this dep in code
langchain-core==1.2.6
langchain-openai==1.1.6
PyJWT[crypto]==2.10.1

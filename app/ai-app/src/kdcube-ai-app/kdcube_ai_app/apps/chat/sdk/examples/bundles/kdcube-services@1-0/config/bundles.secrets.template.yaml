bundles:
  version: "1"
  items:
    - id: "kdcube-services@1-0"
      secrets:
        conversations:
          # HMAC key for short-lived conv/mail/Slack download links and staged
          # upload slots. Generate a distinct high-entropy value per environment.
          file_download_secret: "<RANDOM_HIGH_ENTROPY_SECRET>"

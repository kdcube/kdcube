aiohttp==3.12.15
fastapi==0.116.1
# langchain-openai==0.3.28
langchain==1.2.0
langchain-community==0.4.1
langchain-openai==1.1.6
langchain-core==1.2.6
# langgraph==0.6.1
# langmem>=0.0.4,<0.1.0
langgraph==1.0.5
# Bind a KDCube-served MCP endpoint's tools as LangChain tools for hosted
# LangGraph/LangChain agents (sdk/frameworks/langchain/mcp.py). 0.1.7+ supports
# langchain 1.x. Absent, MCP tools degrade to none and the agent runs plain tools.
langchain-mcp-adapters>=0.1.7
# Pin the split-out prebuilt/checkpoint packages: unpinned, the container resolved a
# newer langgraph-prebuilt whose tool_node imports ExecutionInfo/ServerInfo from a
# newer langgraph.runtime than core 1.0.5 ships -> ImportError on create_react_agent.
# 1.0.8 is the version compatible with langgraph 1.0.5 (used for create_react agents).
langgraph-prebuilt==1.0.8
langgraph-checkpoint==3.0.1
# langgraph-checkpoint-postgres — the durable Postgres checkpointer (AsyncPostgresSaver)
# a ported LangGraph agent opens so its per-conversation history survives restarts and
# is restored when a conversation is reloaded. Pinned to the release compatible with
# langgraph-checkpoint 3.0.1: unpinned it resolves a newer build that requires
# langgraph-checkpoint >=4, which conflicts with langgraph 1.0.5's <4.0.0 ceiling.
# Pulls psycopg-pool transitively. Without it the checkpointer degrades to an in-memory
# saver (no cross-turn history) — see docs/sdk/solutions/conversation/hosted-agent-conversation-README.md.
langgraph-checkpoint-postgres==3.0.5
langmem==0.0.30
dramatiq==1.18.0
PyJWT[crypto]==2.10.1
pydantic==2.11.7
python-dotenv==1.1.1
PyYAML==6.0.2
python-socketio==5.13.0
redis==6.2.0
requests==2.32.5
uvicorn==0.37.0
networkx
neo4j
asyncpg
psycopg2-binary==2.9.10
# psycopg v3 — required by LangGraph's Postgres checkpointer (AsyncPostgresSaver)
# and the optional langgraph_store backend (AsyncPostgresStore). Coexists with
# psycopg2-binary + asyncpg. Ported LangGraph agents' memory/KB use asyncpg (the
# shared pool); only these framework stores need psycopg v3.
psycopg[binary]>=3.1
ddgs
semantic-kernel
reportlab
-r requirements-aws.txt

bs4
playwright
markdown-it-py
mdit-py-plugins
linkify-it-py
pygments
jinja2

Pillow
lxml
tiktoken

pandas
matplotlib

python-magic

pymupdf
pdfminer.six
pypdf
python-docx
python-pptx
chardet
orjson

plotly
kaleido
graphviz
seaborn
diagrams
pyvis
python-dateutil
xlsxwriter
openpyxl
httpx
trio
tinycss2
python-pptx

rich
tenacity
aiolimiter
beautifulsoup4
brotli
markdownify
trafilatura

clamd
aiofiles

croniter
python-multipart

# Geospatial libraries
geopy
folium

stripe==14.3.0
mcp

# faiss vector backend for the generic hybrid index (infra/index/faiss); used by any bundle.
faiss-cpu==1.14.3

# Test utilities for the chat processor venv.
pytest
pytest-asyncio
coverage

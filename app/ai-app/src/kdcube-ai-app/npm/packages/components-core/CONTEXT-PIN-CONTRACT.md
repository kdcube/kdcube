# Context-pin contract

The contract for "a draggable reference to an object" is implemented in
`src/shared/contextPin.ts` (`ContextItem`, `buildContextDrag`, `parseContextDrop`) —
that code is the source of truth.

Human documentation:
`app/ai-app/docs/sdk/npm/components-core/context-pin-contract-README.md`.

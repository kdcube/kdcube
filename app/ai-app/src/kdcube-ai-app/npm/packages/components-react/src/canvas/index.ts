/**
 * @kdcube/components-react/canvas — the React canvas.
 *
 * Re-exports the framework-agnostic logic from @kdcube/components-core/canvas
 * (model, ingress, types, context normalizers, ids) alongside the React view
 * `CanvasBoard`, so consumers keep a single import surface.
 */
export * from '@kdcube/components-core/canvas'
export * from './CanvasBoard'

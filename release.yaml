platform:
  repo: "kdcube-ai-app"
  ref: "2026.7.10.1935"
  description: |
    This release follows 2026.7.10.1730 on the same day with one fix: the
    connect hint raises the consent banner — a named-service connect hint in a
    tool result now surfaces the scoped connected-account consent card in chat
    instead of remaining agent-visible only, keeping the reactive consent path
    uniform across every way an unmet claim can appear.
config:
  version: "2026.7.10.1935"
